{
  "model": "gemini-3.7-flash-high",
  "permissions": {
    "allow": [
      "read_file(*)",
      "command(pwd)",
      "command(wc)",
      "command(ls)",
      "command(cat)",
      "command(head)",
      "command(tail)",
      "command(stat)",
      "command(file)",
      "command(git)"
    ]
  }
}
